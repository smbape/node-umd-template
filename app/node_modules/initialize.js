// where to get the Application class
import Application from "./configs/Application";

const app = new Application();
// window.appConfig is defined in app/assets/index.jst
app.start(window.appConfig);
module.exports = app;
